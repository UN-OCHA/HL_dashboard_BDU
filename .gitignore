# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
.claude/

# Build / cache
scripts/.cache/
__pycache__/
*.pyc

# Local dev
.venv/
venv/
node_modules/

# Skill installs (npx skills add) — keep the lockfile, not the install dir.
# Re-run `npx skills add nutlope/hallmark` (or any other listed skill) to
# restore the actual skill files locally.
.agents/
